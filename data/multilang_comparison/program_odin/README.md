# program_odin Program (Odin)

This program was generated by the **BenchGen** tool.

## Compilation

To compile the program, run the build script:

    ```bash
    ./build.sh
    ```

    The executable will be named `program_odin`.

## Run

To run the program:

```bash
./run.sh
```

To run with arguments:

```bash
./run.sh -path-seed 123
```

### Optional Arguments

-   `-path-seed <seed>`: Sets the seed for the random number generator. Default is `0`.
