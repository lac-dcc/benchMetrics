# program_zig Program (Zig)

This program was generated by the **BenchGen** tool.

## Compilation

To compile the program, use the Zig build system:

    ```bash
    zig build
    ```

    The executable will be located at `zig-out/bin/program_zig`.

## Run

To compile and run the program at once:

```bash
zig build run
```

To run the compiled program with arguments:

```bash
./zig-out/bin/program_zig -loops-factor 50 -path-seed 123
```

### Optional Arguments

-   `-path-seed <seed>`: Sets the seed for the random number generator. Default is `0`.

-   `-loops-factor <factor>`: Sets the factor for the number of loops. Default is `100`.

