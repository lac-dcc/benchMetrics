# program_d Program

This program was generated by the **BenchGen** tool.

## Compilation

There are two ways to compile the program:

1. Standard Compilation:

    ```bash
    make
    ```

    This will create the executable file `program_d`

2. Debug Compilation:

    ```bash
    make debug
    ```

    This will create an executable with debug mode enabled, which prints additional debugging information.

3. LLVM Compilation (requires ldc2):

    ```bash
    make llvm
    ```

    This will produce `.ll` LLVM IR files.

## Run

To run the program, execute the following command:

```bash
./program_d
```

### Optional Arguments

This program accepts the following optional arguments:

-   `-path-seed <seed>`: Sets the seed for the random number generator. Default is `0`.

-   `-loops-factor <factor>`: Sets the factor for the number of loops. Default is `100`.

#### Example:

```bash
./program_d -loops-factor 50 -path-seed 123
```